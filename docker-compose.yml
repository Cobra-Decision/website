services:
  website:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: cobradecision-web
    restart: unless-stopped
    ports:
      - "${PORT:-3000}:3000"
    environment:
      - NODE_ENV=${NODE_ENV:-production}
      - PORT=3000
      - JWT_SECRET=${JWT_SECRET}
      - ALTCHA_HMAC_SECRET=${ALTCHA_HMAC_SECRET}
      - SEED_ADMIN_EMAIL=${SEED_ADMIN_EMAIL}
      - SEED_ADMIN_PASSWORD=${SEED_ADMIN_PASSWORD}
      - STORAGE_DIR=/app/public/uploads
      - ASSET_BASE_URL=/uploads
      - LOG_DIR=/app/log
      - GMAIL_USER=${GMAIL_USER}
      - GMAIL_APP_PASSWORD=${GMAIL_APP_PASSWORD}
      - SMTP_HOST=${SMTP_HOST:-smtp.gmail.com}
      - SMTP_PORT=${SMTP_PORT:-465}
      - SMTP_USER=${SMTP_USER}
      - SMTP_PASS=${SMTP_PASS}
      - SMTP_FROM=${SMTP_FROM}
      - MEET_REMINDER_DAYS_BEFORE=${MEET_REMINDER_DAYS_BEFORE:-1}
      - BASE_URL=${BASE_URL:-http://localhost:3000}
    volumes:
      # Persistent SQLite database file
      - ./app.sqlite:/app/app.sqlite
      # Persistent uploaded assets and materials
      - ./public/uploads:/app/public/uploads
      # Persistent log directory
      - ./log:/app/log
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://localhost:3000/ || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
